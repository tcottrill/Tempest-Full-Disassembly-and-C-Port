# Text files are normalised to LF in the repository and checked out in
# the platform's native line endings; the generated listings included.
* text=auto

# Never normalise these.
*.exe binary
*.zip binary
*.bin binary
*.png binary
